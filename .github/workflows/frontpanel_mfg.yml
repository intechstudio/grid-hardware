# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
  push:
    paths:
      - 'Mechanical/Assembly/Frontpanel/export_all_to_step.py'
      - 'Mechanical/Assembly/Frontpanel/grid_frontpanel_new.FCStd'
      - '.github/workflows/frontpanel_mfg.yml'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  FreeCAD:
    runs-on: ubuntu-latest
    container: "amrit3701/freecad-cli:latest"
    steps:
    - uses: actions/checkout@v2
    - name: Export step files
      run: |
        echo "Hello ${{ github.event.inputs.name }}"
        mkdir temp
        ls
        FreeCADCmd "Mechanical\Assembly\Frontpanel\export_all_to_step.py"
        
    - name: Set Date
      run: echo "action_date=$(date +'%Y-%m-%d')" >> $GITHUB_ENV
      
    - name: Zipping artifacts      
      uses: papeloto/action-zip@v1
      with:
        files: temp/
        dest: "frontpanel_${{ env.action_date }}.zip"
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: "frontpanel_${{ env.action_date }}"
        path: temp/*.*
      
    - uses: tsickert/discord-webhook@v4.0.0
      with:
        webhook-url: ${{ secrets.WEBHOOK_URL }}
        content: "Hello, your files are ready!"
        filename: "frontpanel_${{ env.action_date }}.zip"
