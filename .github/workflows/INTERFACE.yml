name: "KiCad CI/CD for INTERFACE"

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  workflow_dispatch:
  push:
    paths:
      - 'Electrical/Design/INTERFACE/INTERFACE/*.sch'
      - 'Electrical/Design/INTERFACE/INTERFACE/*.kicad_pcb'
      - 'Electrical/Design/INTERFACE/INTERFACE/*.kibot.yaml'
      - 'Electrical/Design/INTERFACE/INTERFACE/Makefile'
      - '.github/workflows/INTERFACE.yml'
      - '.github/workflows/convert_bottom_position.py'
  pull_request:
    paths:
      - 'Electrical/Design/INTERFACE/INTERFACE/*.sch'
      - 'Electrical/Design/INTERFACE/INTERFACE/*.kicad_pcb'
      - 'Electrical/Design/INTERFACE/INTERFACE/*.kibot.yaml'
      - 'Electrical/Design/INTERFACE/INTERFACE/Makefile'
      - '.github/workflows/INTERFACE.yml'
      - '.github/workflows/convert_bottom_position.py'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:

  FabPCB:
    name: PCB fabrication files
    runs-on: ubuntu-latest
    container: "setsoft/kicad_auto:latest"

    steps:
    - uses: actions/checkout@v2

    - name: Run PCB stuff
      run: |
        make -C Electrical/Design/INTERFACE/INTERFACE

    - name: Retrieve results
      if: ${{ always() }}
      uses: actions/upload-artifact@v2
      with:
        name: FabPCB_Output
        path: |
          Electrical/Design/INTERFACE/INTERFACE/mfg-bot/
          Electrical/Design/INTERFACE/INTERFACE/kibot_error.log
        if-no-files-found: warn
        
    - name: Set Date
      run: echo "action_date=$(date +'%Y-%m-%d-%H%M')" >> $GITHUB_ENV

    - name: Convert position file
      run: |
        python3 .github/workflows/convert_bottom_position.py ../../Electrical/Design/INTERFACE/INTERFACE/mfg-bot/mfg/INTERFACE-both_pos.csv ../../Electrical/Design/INTERFACE/INTERFACE/mfg-bot/mfg/INTERFACE-both_pos_fixed.csv
        rm Electrical/Design/INTERFACE/INTERFACE/mfg-bot/mfg/INTERFACE-both_pos.csv
      
    - name: Zipping artifacts      
      uses: vimtor/action-zip@v1
      with:
        files: Electrical/Design/INTERFACE/INTERFACE/mfg-bot/
        dest: "INTERFACE_${{ env.action_date }}.zip"
        
    - uses: tsickert/discord-webhook@v4.0.0
      with:
        webhook-url: ${{ secrets.WEBHOOK_URL }}
        content: "Heyo, fresh PCB files are ready!"
        filename: "INTERFACE_${{ env.action_date }}.zip"
        
    - uses: tsickert/discord-webhook@v4.0.0
      with:
        webhook-url: ${{ secrets.WEBHOOK_URL }}
        content: "Don't forget to check the kibot report!"
        filename: "Electrical/Design/INTERFACE/INTERFACE/kibot_error.log"
